@page "{id:int}"
@model SheduleMatchWeb.Pages.Clubs.UpdateClubModel
@{
}
<div class="mb-5 text-center">
    <div class="container">
        <h1>Aktualizacja klubu</h1>
    </div>
</div>


    @{
        var messageValidation = (string)ViewData["MessageValidation"];
    }

    @if (!string.IsNullOrWhiteSpace(messageValidation))
    {
        <div class="container mb-5">
            <div class="alert alert-danger alert-dismissible fade show mb-5" role="alert">
                @Html.Raw(messageValidation)
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        </div>

    }
    @*@if (Model.ClubUpdate != null)
    {*@
        <div>
            <div class="container">
                <form method="post">
                    <div class="mb-3">
                        <label for="Id" class="form-label">Id</label>
                        <input type="text" class="form-control" id="id" asp-for="ClubUpdate.Id" readonly>
                    </div>
                    <div class="mb-3">
                        <label for="heading" class="form-label">Nazwa</label>
                        <input type="text" class="form-control" asp-for="ClubUpdate.Name">
                    </div>

                    <div class="mb-3">
                        <label for="pageTitle" class="form-label">Rok założenia</label>
                        <input id="Data" type="date" class="form-control" asp-for="ClubUpdate.DateCreated">
                    </div>
                    <div class="form-group">
                        <label for="GameClassId" class="form-label">Klasa rozgrywkowa</label>
                        <select class="form-select" aria-label="Default select example" asp-for="ClubUpdate.GameClassId" asp-items="@(ViewData["klasyRozgrywkowe"] as IEnumerable<SelectListItem>)"></select>
                        <span asp-validation-for="GameClassess" class="text-danger"></span>
                    </div>
                    @*<div class="form-group">
                        <select class="form-select" aria-label="Default select example" asp-for="ClubUpdate.GameClassId" asp-items="@(ViewData["klasyRozgrywkowe"] as IEnumerable<SelectListItem>)"></select>
                        <span asp-validation-for="GameClassess" class="text-danger"></span>
                    </div>*@
@*                     <div class="mb-3">
                        <label for="featuredImageUrl" class="form-label">Featured Image Url</label>
                        <input type="text" class="form-control" id="featuredImageUrl" asp-for="ClubUpdate.FeaturedImageUrl">
                    </div> *@
            @if (User.IsInRole("Admin"))
            {
                <div class="form-group">
                    <label for="UserId" class="form-label">Prezes</label>
                    <select class="form-select" aria-label="Default select example" asp-for="ClubUpdate.UserId" asp-items="@(ViewData["Users"] as IEnumerable<SelectListItem>)"></select>
                    <span asp-validation-for="Users" class="text-danger"></span>
                </div>
            }
            @if (User.IsInRole("President"))
            {
                <div class="form-group">
                    <label for="UserId" class="form-label">Prezes</label>
                        <input type="text" class="form-control" id="id" asp-for="ClubUpdate.UserId" readonly>
                    <span asp-validation-for="Users" class="text-danger"></span>
                    </div>
            }
            <div class="mb-3 d-flex justify-content-between">
                        <button class="btn btn-primary" type="submit" asp-page-handler="Update">Aktualizuj</button>
                        <button class="btn btn-danger" type="submit" asp-page-handler="Delete">Usuń</button>
                    </div>
                </form>
            </div>
        </div>
   @* }*@
